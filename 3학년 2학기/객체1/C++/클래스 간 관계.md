- 파일 분리 시 클래스 안에 다른 클래스가 멤버변수로 있다면
```c++
camera.h
class Lens; -> 빈 클래스 선언
class Camera{
	Lens *lens;
};
```

```c++
camera.cpp

#include "lens.h"
#include "camra.h" 
-> 헤더 두개 include

void Camera::
```
